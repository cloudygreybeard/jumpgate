version: "2"

linters-settings:
  errcheck:
    exclude-functions:
      - (strings.Builder).WriteString
      - (*bytes.Buffer).ReadFrom
      - io.Copy
      - fmt.Sscanf

formatters:
  enable:
    - gofmt

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
