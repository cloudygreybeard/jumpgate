linters-settings:
  errcheck:
    exclude-functions:
      - (strings.Builder).WriteString
      - (*bytes.Buffer).ReadFrom
      - io.Copy
      - fmt.Sscanf

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
